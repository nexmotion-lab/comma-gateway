apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - cluster-issuer.yaml

patchesStrategicMerge:
  - patch-env.yaml
  - patch-ingress.yaml

configMapGenerator:
  - behavior: merge
    files:
      - application-prod.yml
    name: gateway-config


